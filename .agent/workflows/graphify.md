---
name: graphify
description: Turn any folder of files into a navigable knowledge graph
---

# Workflow: graphify

Follow the reviewed repository skill at `.github/skills/graphify/SKILL.md`.
The `graphifyy` package is pinned in `pyproject.toml` dev dependencies; do not
install or upgrade tools from this workflow.

If no path argument is given, use `.` (current directory).
