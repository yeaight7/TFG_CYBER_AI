% Figura conceptual: no codifica resultados medidos.
% Fuente: experiments/final_experiment_campaign.json y protocolo de la memoria.
\begin{tikzpicture}[node distance=4mm]
  \node[cajaDato, text width=10.8cm] (random)
    {\textbf{1. Control intradistribución}\\MAIN aleatoria + validación directa + bootstrap + duplicados + etiquetas barajadas};
  \node[cajaProceso, text width=10.8cm, above=of random] (day)
    {\textbf{2. Separación temporal interna}\\entrenamiento lunes--miércoles; prueba jueves--viernes};
  \node[cajaProceso, text width=10.8cm, above=of day] (holdout)
    {\textbf{3. Escenarios retenidos dirigidos}\\Web Attacks, Infiltration, PortScan y DDoS};
  \node[cajaModelo, text width=10.8cm, above=of holdout] (phase2)
    {\textbf{4. Cambio de fuente}\\inferencia offline sobre la captura cerrada de laboratorio};

  \draw[flechaAzul] (random) -- (day);
  \draw[flechaAzul] (day) -- (holdout);
  \draw[flechaAzul] (holdout) -- (phase2);

  \node[etiqueta, rotate=90, text width=4.2cm, left=8mm of day]
    {mayor separación entre entrenamiento y evaluación};

  \node[cajaPendiente, text width=2.7cm, right=7mm of day] (stability)
    {\textbf{Estabilidad}\\tamaño + semilla};
  \node[etiquetaTenue, text width=2.8cm, below=1mm of stability]
    {caracteriza el modelo; no aumenta por sí sola la validez externa};
  \draw[flechaFina] (stability.west) -- (day.east);
\end{tikzpicture}
