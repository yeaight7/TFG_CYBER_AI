% Figura conceptual: no codifica resultados medidos.
% Fuente: scripts/predict_real_traffic_v2.py y contrato de campaña.
\begin{tikzpicture}[node distance=5mm and 8mm]
  \node[cajaModelo, text width=5.2cm] (main)
    {\textbf{MAIN final validada}\\modelo + scaler train-only + percentiles + nombres de características};
  \node[cajaDato, text width=5.2cm, right=of main] (lab)
    {\textbf{Captura de laboratorio}\\flujos + hash de entrada + etiquetas cuando están disponibles};

  \node[cajaProceso, text width=11.3cm, below=8mm of $(main.south)!0.5!(lab.south)$] (mapping)
    {Mapeo al contrato canónico $76+76$ $\rightarrow$ saneamiento y recorte configurado $\rightarrow$ escalado sin reajuste};
  \node[cajaModelo, text width=11.3cm, below=of mapping] (predict)
    {Predicción QRDQN determinista y evaluación offline};
  \node[cajaResaltada, text width=11.3cm, below=of predict] (outputs)
    {Predicciones + métricas + diagnósticos de presencia y cambio de distribución + manifiesto};

  \draw[flechaAzul] (main) -- (mapping);
  \draw[flechaAzul] (lab) -- (mapping);
  \draw[flecha] (mapping) -- (predict);
  \draw[flecha] (predict) -- (outputs);

  \node[etiquetaTenue, text width=11.3cm, below=3mm of outputs]
    {\texttt{BLOCK} es una etiqueta escrita en artefactos; no se ejecuta ninguna acción sobre la red.};
\end{tikzpicture}
