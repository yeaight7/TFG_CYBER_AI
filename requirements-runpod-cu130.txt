# Scope: minimal RunPod GPU runtime for training/eval (CUDA cu130). Mirrors the
# core pyproject.toml dependencies plus training-only extras (tensorboard,
# matplotlib). It INTENTIONALLY omits the `dev` extra (pytest/ruff/graphifyy)
# and the `tune` extra (optuna) — they are not needed to train or evaluate.
# To run hyperparameter tuning (src/tune_hparams.py) on the box, also install
# optuna==4.9.0 (see the `tune` extra in pyproject.toml).
--extra-index-url https://download.pytorch.org/whl/cu130

numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
gymnasium==1.2.3
torch==2.12.0+cu130
stable-baselines3==2.8.0
sb3-contrib==2.8.0
joblib==1.5.3
tensorboard==2.20.0
matplotlib==3.10.9
