# misc
venv/
.venv/
*.pyc
__pycache__/
.ipynb_checkpoints/
**/.ipynb_checkpoints/
*.aux
*.bbl
*.bcf
*.blg
report/*.log
*.log
*.out
*.run.xml
*.toc
*.lof
*.lot
*.loa
*.gz
*.fdb_latexmk
*.fls
*-SAVE-ERROR
.vscode/
.remember/
report/tmp
.python-version

# PCAPS and big CSV files
datasets/CICIDS2017/Raw_dataset/
synthetic*
lab_capture_traffic*
runs/archive/phase2/P2v2_pred_20260412_004102/predictions.csv
runs/archive/phase2/P2v2_pred_20260412_004624/predictions.csv
runs/phase2/**/predictions_sensitive_local.csv
runs/archive/phase2/**/predictions_sensitive_local.csv
runs/cicids2017/*/checkpoints/
runs/**/events.out.tfevents.*
runs/**/model.zip
scratch/

# Generated canonical dataset caches (rebuildable; never commit shard arrays).
/cache/
/.cache/

# large models type shi
models/rf_cicids2017_canonical_20260504_000432.joblib

# Archived heavy binaries de-tracked 2026-07 (kept on local disk and in git
# history per D-6 no-rewrite; the archive READMEs stay tracked as the record).
# NOTE: `dir/*` form (not `dir/`) so the README negations below work.
models/archive/*
!models/archive/README.md
pcaps/archive/*
!pcaps/archive/README.md

# Legacy NSL-KDD dropped (decision D-8): kept out of the repo going-forward.
datasets/nsl_kdd/
models/rf_nslkdd.joblib

# Graphify output files — untracked entirely (generated semantic cache, may be
# stale; e.g. a fabricated reward-config node). Regenerate locally with `graphify .`.
.graphify_python
.graphify_*.json
graphify-out/
graphify-out-*/

# Personal research notes (docs/research/personal/): tracked as Markdown only (single source format).
# Derived .tex/.pdf were removed to avoid 3 copies of the same research notes.

# idk what this is
dev/
